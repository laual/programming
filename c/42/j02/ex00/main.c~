#include <unistd.h>
#include <stdio.h>

int	ft_putchar (char c);
void	ft_print_alphabet(void);

int	main (void)
{
    
    return (0);
}

int	ft_putchar(char c)
{
    write(1, &c, 1);
    return (0);
}

int	ft_print_alphabet(void)
{
    int i;
    i = 97;
    
    while (i < 
}
